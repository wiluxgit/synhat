all packs must be declared in "packs.json"
example of packs file:
```
{
	"packs":[
		{"path":"example", "author":"Wilux"}
	]
}
```
required tags:
 `path` the folder name located in this directory
optional tags:
 `author` the author name/username

